{
  "name": "gestor-inmobiliario-api",
  "version": "1.0.0",
  "description": "API REST del Gestor Inmobiliario: autenticación, perfil y agenda de tareas.",
  "main": "src/server.js",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test --test-concurrency=1 \"tests/**/*.test.js\""
  },
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "cors": "^2.8.5",
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.18.0",
    "nodemailer": "^10.0.4"
  },
  "devDependencies": {
    "supertest": "^7.1.4"
  }
}
